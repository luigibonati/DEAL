name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version:
          - "3.10"
          - "3.12"
          - "3.14"
    defaults:
      run:
        shell: bash -l {0}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Conda (Python ${{ matrix.python-version }})
        uses: conda-incubator/setup-miniconda@v3
        with:
          activate-environment: deal
          python-version: ${{ matrix.python-version }}
          auto-update-conda: true
          channels: conda-forge,defaults

      - name: Install DEAL build dependencies
        run: |
          conda install -y pip cmake ninja scikit-build-core pybind11 nlohmann_json eigen c-compiler cxx-compiler openmp liblapacke openblas -c conda-forge

      - name: Install DEAL
        run: |
          python -m pip install . --no-build-isolation

      - name: Run DEAL tests
        run: |
          cd tests
          bash run_all.sh
